{
  "name": "tailwind",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -b && vite build",
    "build:electron": "cross-env MODE=electron tsc -b && cross-env MODE=electron vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.5",
    "tailwindcss": "^4.1.5"
  },
  "dependencies": {
    "lucide-react": "^0.523.0"
  }
}
